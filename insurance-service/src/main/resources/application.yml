spring:
  application:
    name: insurance-service
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${KEYCLOAK_ISSUER_URI:http://localhost:8080/realms/insurance}
          jwk-set-uri: ${KEYCLOAK_JWK_SET_URI:http://localhost:8080/realms/insurance/protocol/openid-connect/certs}

server:
  port: 8081

management:
  endpoints:
    web:
      exposure:
        include: health
